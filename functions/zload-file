# to be sourced!!!
#! /usr/bin/zsh -f

zload-file ()
{
    file=$1
    setopt nomonitor
    coproc tac $file &!
    while read line;
    do
	# if $verbose; then echo pushing $line;fi
	builtin print -z $line
    done  <& p
    wait
    # sorry:
    setopt monitor
}
